// Minimal module imported by the test-runner smoke test (see __tests__/smoke.test.ts)
// to prove the @domain/* alias resolves inside Jest, not just tsc/Metro.
export const domainAliasSmokeValue = 'domain-alias-ok';
